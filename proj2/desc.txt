в папке tamplate лежит html форма, которая является основой приложения

в web.py лежит фласк, а forecast.py основные функции приложения которые я импортирую в web.py

по поводу дебага:

фактически есть два места где может быть ошибку: в web.py или в forecast.py; в обоих файлах все оишбки ловятся try, фактически весь код в него обернут. есть несколько ошибок которые могут произойти: проблемы на сервере accuweather, проблемы с формой html или flask